[build]
builder = "NIXPACKS"
buildCommand = "pip install --upgrade pip && pip install python-telegram-bot==20.7"

[deploy]
startCommand = "python bot.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
